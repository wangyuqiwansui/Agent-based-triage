---
name: harness-engineering-patterns
description: Use when a request explicitly needs Agent Harness architecture analysis, cognition x topology mapping, workflow or engineering node diagnosis, recoverable Plan-and-Execute, a governed reasoning, reflection, or tool-dispatch execution flow, workflow observability probes, pattern governance, evidence-backed EIR, or Skillization decisions. / 当请求明确需要 Agent Harness 架构分析、认知 x 拓扑映射、工作流或工程节点诊断、可恢复的计划并执行、受治理的推理、反思或工具调度执行流程、工作流可观测性探针、模式治理、有证据的 EIR 或 Skill 化决策时使用。
---

# Harness Engineering Patterns / Harness 工程模式

## Overview / 概览

Compile a Workflow / Harness Source / 工作流程 / Harness 源码 into an Engineering Intermediate Representation / 工程中间表示, map it onto cognition x topology coordinates, and produce Evidence + Evaluation + Governance / 证据 + 评估 + 治理 decisions that can be verified and maintained. / 将工作流程或 Harness 源码编译为工程中间表示，映射到认知 x 拓扑坐标，并产出可验证、可维护的证据、评估与治理决策。

Treat `references/registry.json` as the authoritative structural source. Treat the matrix, catalog, cell guides, design files, observability files, and HTML as maintained views or detailed guidance. / 将 `references/registry.json` 视为权威结构数据源；将矩阵、目录、纵轴导论、设计文件、可观测性文件和 HTML 视为维护视图或详细指导。

## When To Use / 适用场景

- Analyze an Agent Harness repository, runtime, tool system, memory system, sandbox, permission layer, subagent system, or event log. / 分析 Agent Harness 仓库、runtime、工具系统、记忆系统、沙箱、权限层、子 Agent 系统或事件日志。
- Design or verify Action → Tool Dispatch with capability frontiers, execution admission, durable idempotency, result certainty, and lifecycle observability. / 设计或验证具备能力前沿、执行准入、持久幂等、结果确定性和生命周期可观测性的“行动 → 工具调度”。
- Design or verify governed reflection with explicit admission, a frozen baseline, scoped authorization, independent revalidation, regression protection, anti-gaming, bounded stopping, and evidence-backed learning promotion. / 设计或验证具备明确准入、冻结基线、受限授权、独立复验、回归保护、反投机、有界停止与证据化学习晋升的受治理反思。
- Diagnose a workflow or engineering node whose capability, topology, feedback, memory, ownership, or governance is unclear. / 诊断能力、拓扑、反馈、记忆、归属或治理不清的工作流或工程节点。
- Produce an EIR, matrix mapping, pattern card, evidence table, governance gap, evaluation, or Skillization decision. / 生成 EIR、矩阵映射、模式卡、证据表、治理缺口、评价或 Skill 化决策。
- Evaluate whether repeated workflow evidence justifies promoting an extension candidate into a named pattern. / 评价反复工作流证据是否足以把扩展候选晋升为命名模式。

## When Not To Use / 不适用场景

- Ordinary code review that does not need Harness architecture mapping. / 不需要 Harness 架构映射的普通代码评审。
- Isolated bug diagnosis where the task is to find one root cause, not classify a workflow. / 只需定位单一根因、不需要工作流分类的单点缺陷诊断。
- General product or UX workflow critique that does not need the cognition x topology matrix. / 不需要认知 x 拓扑矩阵的通用产品或 UX 流程评审。
- Simple implementation, rewriting, formatting, or factual questions with no reusable pattern decision. / 不涉及可复用模式决策的简单实现、改写、格式调整或事实问答。

Use the focused domain Skill instead when another Skill directly covers the task. / 当其他领域 Skill 能直接覆盖任务时，使用更聚焦的领域 Skill。

## Compiler Spine / 编译主线

1. Read `references/compiler-workflow.md` and classify the input as `workflow`, `harness_source`, or `mixed`. / 读取 `references/compiler-workflow.md`，将输入分类为 `workflow`、`harness_source` 或 `mixed`。
2. Read `references/eir-schema.md` and maintain evidence-backed nodes, components, flows, mappings, patterns, evaluations, and governance items. / 读取 `references/eir-schema.md`，维护有证据的节点、组件、流、映射、模式、评价和治理项。
3. For workflow or engineering-node diagnosis, use Trace Insert / Trace 插入 before Pattern Selection Card / 模式选型卡. / 对工作流或工程节点诊断，先执行 Trace 插入，再运行模式选型卡。
4. For a reasoning runtime, read `references/reasoning-execution-flow.md` before the selected reasoning cell; use the contracts under `schemas/` as normative, `runtime/reasoning_router.py` as the deterministic route reference, `runtime/reasoning_runtime.py` as the executable semantics and event-conformance kernel, and `runtime/reasoning_artifacts.py` as the producer-side hash, Schema, cross-binding, and evidence-gate guard; establish identities, input and reasoning contracts, budgets, validators, switches, stop conditions, release-bound public claims, authoritative gate time, and terminal output without collecting private chain-of-thought. When the selected cell is `reasoning-chain`, also read `references/reasoning-chain-factory.md`; compile the explicit blueprint and sealed contract through `runtime/reasoning_chain_factory.py` using the blueprint, plan, and checkpoint-validation Schemas; retain the original blueprint and contract for deterministic session reconstruction, and never bypass `ChainPlanSession` for step or candidate execution. Supply versioned, self-hashed evidence records rather than bare references, reserve the exact compiled allocation before step dispatch, and settle actual use against that reservation at close. / 对推理运行时，先读取 `references/reasoning-execution-flow.md`，再读取已选推理单元；以 `schemas/` 下的契约为规范，以 `runtime/reasoning_router.py` 为确定性路由参考，以 `runtime/reasoning_runtime.py` 为可执行语义与事件一致性内核，以 `runtime/reasoning_artifacts.py` 为生产端哈希、Schema、跨绑定与证据门闸门；建立标识、输入与推理契约、预算、验证器、换路、停止条件、放行绑定的公开声明、权威闸门时间和终态输出，且不采集私密思维过程。所选单元为 `reasoning-chain` 时，还要读取 `references/reasoning-chain-factory.md`；使用蓝图、计划与检查点验证 Schema，通过 `runtime/reasoning_chain_factory.py` 编译显式蓝图和密封契约；保留原始蓝图与契约以便确定性重建会话，且步骤与候选执行均不得绕过 `ChainPlanSession`。应提供带版本和自哈希的证据记录而非裸引用，在步骤分派前预留编译出的精确预算，并在关闭时针对该预留结算实际用量。
   When the selected cell is `reasoning-parallel`, also read `references/reasoning-parallel-factory.md`; compile `schemas/reasoning-parallel-blueprint.schema.json` plus the sealed contract through `runtime/reasoning_parallel_factory.py`, retain the source artifacts, and never bypass `ParallelPlanSession`. Open new runs with `start_session()` and durable event-backed runs with `resume_session()`; reserve the complete branch wave atomically before the first launch; keep branch namespaces isolated until close; require an explicit terminal for every planned path; compare candidates under one common versioned contract; and preserve the complete branch manifest, minority findings, and elimination reasons. Derive branch inventories and registered metric inputs only through `runtime/reasoning_parallel_projection.py`. Reference v1 accepts public branch outcomes but performs no tools or side effects; action authorization remains separate. / 所选单元为 `reasoning-parallel` 时，还要读取 `references/reasoning-parallel-factory.md`；将 `schemas/reasoning-parallel-blueprint.schema.json` 与已封存契约通过 `runtime/reasoning_parallel_factory.py` 编译，保留源制品，且不得绕过 `ParallelPlanSession`。新运行使用 `start_session()`，持久化事件支持的运行使用 `resume_session()`；首个分支启动前原子预留完整分支波次；分支关闭前保持命名空间隔离；要求每条计划路径都有显式终态；在统一版本化契约下比较候选；并保留完整分支清单、少数结论和淘汰原因。分支清单及已注册指标输入只能通过 `runtime/reasoning_parallel_projection.py` 派生。参考实现 v1 只接收公开分支结果，不执行工具或副作用；行动授权保持独立。
   For externally dispatched parallel workers, construct `ParallelPathScheduler` from `runtime/reasoning_parallel_scheduler.py`, bind every lease to one plan, path, step, worker, revision, expiry, and optional global deadline, and call `sweep_due()` before reassignment. Lease TTL expiry revokes worker ownership but keeps the logical branch and its reservation open; only a global deadline closes open paths under the compiled policy. Reacquire the path with a new lease, and submit public branch outcomes only through `close_leased_branch()`, which atomically verifies the current unexpired holder, releases its lease, and closes the branch. Apply `on_deadline` exactly; `proceed_with_quorum` never waives the configured minimum, while `escalate` and `fail` are terminal. After `synthesize()` selects a candidate, call `finalize_selected_candidate()` with externally obtained validator outcomes, the release-bound claims, decision, output, and provenance; publish only the returned sealed normative result. / 对外部分派的并行工作者，使用 `runtime/reasoning_parallel_scheduler.py` 中的 `ParallelPathScheduler`，把每条租约绑定到一个计划、路径、步骤、工作者、修订、过期时间及可选全局截止时间，并在重新分配前调用 `sweep_due()`。租约 TTL 到期只撤销工作者所有权，逻辑分支及其预算预留保持开放；只有全局截止才按已编译策略关闭开放分支。使用新租约重新领取路径，并且外部公开分支结果只能通过 `close_leased_branch()` 提交；该方法在同一事务中校验当前未过期持有者、释放租约并关闭分支。必须精确执行 `on_deadline`；`proceed_with_quorum` 不得免除配置的最小完成数，`escalate` 与 `fail` 均为终态。`synthesize()` 选出候选后，调用 `finalize_selected_candidate()`，传入外部取得的验证结果、放行绑定声明、决定、输出和来源；只能发布其返回的已封存规范结果。
   For crash-safe dispatch, pair the scheduler with `ParallelDispatchCoordinator` and `SqliteParallelDispatchOutbox` or `PostgresParallelDispatchOutbox`. Acquisition must commit the lease event and immutable work item together; every reacquisition increments a path-local fencing token / 栅栏令牌, and result submission must present the current token. Delivery is at-least-once / 至少一次: expiring delivery claims recover dispatcher crashes, but side-effecting consumers still require their own durable idempotency key or inbox. Never claim end-to-end exactly-once execution. / 崩溃安全分派应把调度器与 `ParallelDispatchCoordinator` 及 SQLite 或 PostgreSQL 发件箱组合使用。获取操作必须共同提交租约事件与不可变工作项；每次重新获取都递增路径局部栅栏令牌，结果提交必须携带当前令牌。交付语义是至少一次：可过期交付领取可恢复分派器崩溃，但产生副作用的消费者仍需自有持久幂等键或 inbox。不得宣称端到端严格一次执行。
   For single-node multi-writer reasoning events and terminal results, use `runtime/reasoning_event_sqlite_store.py`. Its outer transaction takes `BEGIN IMMEDIATE`, reloads and validates the authoritative stream, commits a contiguous suffix, preserves cross-instance idempotency, and rejects a stale causal parent. Schema v2 adds one immutable `terminal_results` row per run and migrates v1 event databases without rewriting events; `health_check()` includes `result_count`. Reload the engine before retrying a stale write. Treat this SQLite adapter as a local reference; remote or horizontal writers still require a network database with equivalent serialization, atomicity, replay, result immutability, migration, backup, and fail-closed behavior. / 单节点多写者推理事件及终态结果使用 `runtime/reasoning_event_sqlite_store.py`。其最外层事务通过 `BEGIN IMMEDIATE` 取得写预留，重载并校验权威流，提交连续后缀，保持跨实例幂等，并拒绝陈旧因果父事件。Schema v2 为每个运行增加一条不可变 `terminal_results` 记录，并且不重写事件即可迁移 v1 事件库；`health_check()` 包含 `result_count`。陈旧写重试前必须重载引擎。该 SQLite 适配器仅是本地参考；远程或水平写者仍需具有等价串行化、原子性、重放、结果不可变性、迁移、备份与默认阻断行为的网络数据库。
   For horizontal event writers, use the Psycopg 3 `PostgresEventStore` reference in `runtime/reasoning_event_postgres_store.py`; it serializes each run with a transaction-scoped advisory lock, reloads the authoritative stream, and persists immutable terminal results under database uniqueness constraints. Pair it with `runtime/reasoning_parallel_postgres_outbox.py`, whose consumers claim rows with `FOR UPDATE SKIP LOCKED`. Run real integration tests only against an isolated database configured by `HARNESS_POSTGRES_DSN`; an absent DSN is a reported skip, never evidence that the network path passed. Deployments still own credentials, TLS, least privilege, pooling, migration rollout, retention, backup/restore, monitoring, and disaster-recovery drills. / 水平事件写者使用 `runtime/reasoning_event_postgres_store.py` 中基于 Psycopg 3 的 `PostgresEventStore` 参考实现；它通过事务级 advisory lock 按运行串行化、重载权威事件流，并在数据库唯一约束下持久化不可变终态结果。与 `runtime/reasoning_parallel_postgres_outbox.py` 组合后，消费者使用 `FOR UPDATE SKIP LOCKED` 领取记录。真实集成测试仅在 `HARNESS_POSTGRES_DSN` 指向隔离数据库时运行；DSN 缺失必须报告为跳过，绝不能作为网络路径已通过的证据。部署方仍负责凭据、TLS、最小权限、连接池、迁移发布、保留、备份恢复、监控与灾难恢复演练。
   The compatibility entrypoint is `runtime/reasoning_chain_factory.py`; static compilation and validation live in `runtime/reasoning_chain_compiler.py`, while guarded execution lives in `runtime/reasoning_chain_session.py`. For durable local events, construct `JsonlEventStore`; it atomically stores immutable terminal results in a self-hashed sibling `.results.json` file, and a reopened engine restores that sealed result without adding lifecycle events. Keep the sealed plan and contract beside the stream, and use a transactional database adapter for high-volume or multi-writer production. Event-terminal and result persistence form a recoverable two-phase boundary: if a crash occurs before the result commit, replay the terminal stream and rebuild; once saved, an exact retry returns the same result and divergent content fails closed. A tool step must bind one exact versioned read-only tool and authorization policy, reserve exactly one call, and pass the same concrete `authorization_binding` through `dispatch_readonly_tool()` and `observe_readonly_tool()`; configure a trusted `tool_authorizer`, fail closed unless it returns exact `true`, and never persist raw tool input/output. After the final checkpoint passes, call `set_candidate(candidate, evidence_records=[...])` with higher-version candidate-bound evidence revisions whose `predecessor_evidence_binding` exactly covers the ordered final-claim step evidence. / 兼容入口为 `runtime/reasoning_chain_factory.py`；静态编译与校验位于 `runtime/reasoning_chain_compiler.py`，受守卫执行位于 `runtime/reasoning_chain_session.py`。本地事件需要持久化时构造 `JsonlEventStore`；它把不可变终态结果原子保存到自哈希的同级 `.results.json` 文件，重新打开的引擎无需新增生命周期事件即可恢复该封存结果。把封存计划与契约同事件流并置保存；高吞吐或多写者生产环境使用事务数据库适配器。事件终态与结果持久化构成可恢复的两阶段边界：若结果提交前崩溃，则重放终态事件流并重新构造；一旦保存，完全重试返回同一结果，分歧内容默认阻断。工具步骤必须绑定一个确切版本的只读工具与授权策略、精确预留一次调用，并向 `dispatch_readonly_tool()` 与 `observe_readonly_tool()` 传入同一个具体 `authorization_binding`；配置可信 `tool_authorizer`，只有精确返回 `true` 才放行，且不得持久化原始工具输入输出。末个检查点通过后，调用 `set_candidate(candidate, evidence_records=[...])`，传入更高版本且绑定候选的证据修订，其 `predecessor_evidence_binding` 必须精确覆盖有序的最终命题步骤证据。
   At the Harness workflow boundary, produce `schemas/workflow-route-envelope.schema.json` through `runtime/workflow_router.py`, freeze exactly 15 typed workflow signals with field provenance, and keep workflow lane, reasoning topology, and action authorization separate. Persist initial decisions and `schemas/workflow-route-revision.schema.json` commits through `runtime/workflow_route_ledger.py`; use its local single-writer JSONL mode only where appropriate and a transactional adapter for multi-writer production. Never treat reasoning `execute` as permission to act, de-escalate from budget alone, mutate a route in place, or replay completed side effects. / 在 Harness 工作流边界，通过 `runtime/workflow_router.py` 生成符合工作流路由信封 Schema 的制品，冻结恰好 15 项带字段来源的类型化工作流信号，并分离工作流车道、推理拓扑与行动授权。通过 `runtime/workflow_route_ledger.py` 持久化初始决定和工作流路由修订 Schema 提交；本地单写者 JSONL 模式只用于合适场景，多写者生产环境使用事务适配器。绝不得把推理 `execute` 当作行动权限、只因预算降级、原地修改路由或重放已完成副作用。
   When the selected cell is `action-chain`, read `references/patterns/action/action-chain.md` and its observability file. Seal a deterministic single-responsibility step sequence; pass state only through typed, versioned, digest-bound artifacts; require format, structure, semantic, and factual gates before registration or continuation; and allow only bounded recovery that records new evidence, constraints, scope, a deterministic check, or a human decision. Treat the documented YAML as a design-level contract until normative Schemas and runtime support exist. Sequence is not authorization: route protected tool actions through Tool Dispatch and governance, never directly retry an unknown side effect, and move to routing, parallel, orchestration, or loop topology when dynamic control dominates. / 所选单元为 `action-chain` 时，读取 `references/patterns/action/action-chain.md` 及其可观测性文件。封存确定性的单一职责步骤序列；状态只通过类型化、版本化、摘要绑定工件传递；工件注册或继续前必须通过格式、结构、语义和事实闸门；恢复必须有界，并记录新证据、新约束、范围变化、确定性检查或人工决定。在规范 Schema 与运行时支持就绪前，文档中的 YAML 只作为设计层契约。顺序不等于授权：受保护工具动作必须经过工具分派与治理，未知副作用不得直接重试；动态控制占主导时应切换到路由、并行、编排或循环拓扑。
   When the selected cell is `action-orchestration`, read `references/patterns/action/action-orchestration.md` and its observability file. Treat `schemas/goal-contract.schema.json`, `schemas/workflow-plan.schema.json`, `schemas/workflow-plan-patch.schema.json`, `schemas/workflow-checkpoint.schema.json`, and `schemas/workflow-execution-result.schema.json` as normative. Use `PlanExecutionSession` from `runtime/plan_execution.py` for deterministic ready-step selection, guarded state transitions, local patch validation, idempotency decisions, and safe restore; use `SqlitePlanExecutionStore` for atomic checkpoint/event/idempotency-snapshot/outbox commits with stale-head rejection; use `PlanToolDispatchCoordinator` for persist-before-dispatch composition with the governed Tool Dispatch runtime; use `PlanExecutionEventAdapter` for validated dual reasoning/tool streams; and use `finalize_workflow_execution()` as the final completion gate. Keep immutable goal and plan revisions separate from mutable step truth; never overwrite `DONE`, expand a patch beyond the failed-and-affected subgraph, retry `UNKNOWN` before verification, treat plan inclusion as tool authorization, claim cross-database atomicity, or publish from mechanical completion alone. / 所选单元为 `action-orchestration` 时，读取 `references/patterns/action/action-orchestration.md` 及其可观测性文件。以目标契约、工作流计划、计划补丁、检查点和工作流执行结果 Schema 为规范。使用 `runtime/plan_execution.py` 中的 `PlanExecutionSession` 实现确定性就绪步骤选择、受保护状态转换、局部补丁校验、幂等决策和安全恢复；使用 `SqlitePlanExecutionStore` 原子提交检查点、事件、幂等快照和 Outbox，并拒绝陈旧头；使用 `PlanToolDispatchCoordinator` 以分派前持久化方式组合受治理工具分派运行时；使用 `PlanExecutionEventAdapter` 生成已校验推理/工具双流；使用 `finalize_workflow_execution()` 作为最终完成闸门。保持不可变目标与计划修订同可变步骤真值分离；不得覆盖 `DONE`、把补丁扩大到失败及受影响子图之外、在核验前重试 `UNKNOWN`、把进入计划视为工具授权、宣称跨数据库原子性，或仅凭机械完成发布结果。
   For single-node multi-writer route persistence, use `runtime/workflow_route_sqlite_ledger.py`: derive the stream key from the sealed envelope, keep one immutable `max_switches` setting per stream, rely on its transaction-scoped full-chain validation and idempotency, migrate legacy JSONL only through `migrate_jsonl()`, and surface `health_check()` to probe self-health. Treat SQLite as a local reference; horizontal or remote-writer deployments must provide a network database adapter with equivalent atomic append-and-head, replay, migration, backup, and fail-closed semantics. / 单节点多写者路由持久化使用 `runtime/workflow_route_sqlite_ledger.py`：从封存信封派生流键，每条流保持一个不可变的 `max_switches` 配置，依赖事务内完整链校验与幂等；旧 JSONL 只通过 `migrate_jsonl()` 迁移，并把 `health_check()` 接入探针自健康。SQLite 仅是本地参考；水平扩展或远程写者部署必须提供具有等价原子追加与头推进、重放、迁移、备份和默认阻断语义的网络数据库适配器。
   When the selected cell is `action-routing`, read `references/tool-dispatch-execution.md` and treat `schemas/tool-dispatch-envelope.schema.json`, `schemas/tool-execution-event.schema.json`, and `schemas/tool-execution-result.schema.json` as normative. Use `runtime/tool_dispatch.py` for capability-frontier compilation, deterministic selection, fourteen ordered admission checks, live authorization, real execution, and result classification. Use `runtime/tool_dispatch_sqlite_store.py` only as the local durable lease/event reference and replace it with an equivalently transactional network store for horizontal writers. Use `runtime/tool_dispatch_projection.py` to rebuild complete inventories and integrity anomalies before computing dispatch metrics. Selection is not authorization; never execute a non-`allow` envelope, never execute a write without a durable idempotency lease, and never directly retry an `unknown` outcome. / 所选单元为 `action-routing` 时，读取 `references/tool-dispatch-execution.md`，并以 `schemas/tool-dispatch-envelope.schema.json`、`schemas/tool-execution-event.schema.json` 和 `schemas/tool-execution-result.schema.json` 为规范。使用 `runtime/tool_dispatch.py` 编译能力前沿、确定性选候选、执行十四项有序准入检查、实时授权、真实执行和结果分类。`runtime/tool_dispatch_sqlite_store.py` 仅作为本地持久租约/事件参考；水平写者必须替换为具有等价事务语义的网络存储。使用 `runtime/tool_dispatch_projection.py` 重建完整清单与完整性异常后再计算调度指标。选中不等于授权；不得执行非 `allow` 信封，不得在无持久幂等租约时执行写动作，不得直接重试 `unknown` 结果。
   When the selected cell is `action-hierarchy`, read `references/patterns/action/action-hierarchy.md` and its observability file together with `references/tool-dispatch-execution.md` and `references/workflow-observability-probes.md`. Treat the Tool Dispatch envelope, event, result Schemas and `runtime/tool_dispatch.py` as the normative executable subset; keep rehearsal and source-lineage contracts, raw-result quarantine and output release, effect reconciliation, and compensation orchestration design-level until the adopting project supplies versioned contracts, emitters, projections, and failure-path tests. Bind approval and permit to the exact canonical action, recheck live authority immediately before the side-effect boundary, and durably record configured high-risk intent and admission facts before crossing it. Preserve admission, execution classification, side-effect certainty, output release, verification, and compensation as orthogonal facts: a POST output block does not undo a confirmed effect, `unknown` or `partial` must be reconciled before retry, and compensation is a new governed action. Never let remote tool annotations widen local authority or let observability deploy its own policy changes. / 所选单元为 `action-hierarchy` 时，同时读取 `references/patterns/action/action-hierarchy.md`、其可观测性文件、`references/tool-dispatch-execution.md` 和 `references/workflow-observability-probes.md`。将 Tool Dispatch 的信封、事件、结果 Schema 与 `runtime/tool_dispatch.py` 视为规范的可执行子集；在采用项目提供版本化契约、发送器、投影器和失败路径测试前，预演与来源血缘、原始结果隔离与输出放行、效果核验及补偿编排保持设计层。审批与许可必须绑定精确规范行动，在副作用边界前立即复核实时权限，并在越界前持久记录策略要求的高风险意图与准入事实。准入、执行分类、副作用确定性、输出放行、核验和补偿必须保持为正交事实：POST 输出阻断不会撤销已确认效果，`unknown` 或 `partial` 必须先核验再重试，补偿是新的受治理行动。不得让远程工具 annotations 扩大本地权限，也不得让可观测系统自行部署策略变更。
   When `action-hierarchy` scope includes replanning, compensation orchestration, or business-level final completion, also read `references/patterns/action/action-orchestration.md` and its observability file. Use Plan-and-Execute only as the outer workflow owner for recovery ordering, compensation dispatch, and completion gating; compensation availability or success never rewrites the original action's execution and side-effect facts. / 当 `action-hierarchy` 范围包含重规划、补偿编排或业务级最终完成时，还必须读取 `references/patterns/action/action-orchestration.md` 及其可观测性文件。Plan-and-Execute 仅作为外层工作流负责人，用于恢复排序、补偿分派和完成门控；补偿可用或成功绝不能改写原行动的执行与副作用事实。
5. For reflection or self-heal execution, read `references/reflection-execution-flow.md` before the selected Reflection cell. Seal `schemas/reflection-contract.schema.json`, emit `schemas/reflection-event.schema.json`, close each round with `schemas/reflection-round-observation.schema.json`, and use `runtime/reflection_runtime.py` plus `ReflectionSession` to enforce admission, metric-and-evidence-bound baseline freeze, proposal/authorization separation, persistent subject versions, never-reused signal and attribution evidence, contract-approved attribution authorities, independent revalidation, approved validator rebasing, regression protection, validator anti-gaming, progress budgets, explicit stopping, verified rollback events, and separately authorized source-linked learning decisions. Pass the complete event stream to contract-level observation validation; an observation hash alone is not history proof. `rolled_back` is legal only after the contract rollback binding is applied, the before-subject is restored, and mandatory validators bind recovery evidence. The runtime coordinates public evidence and state only; it does not perform a repair, grant action authority, or collect private reasoning. / 对反思或自愈执行，先读取 `references/reflection-execution-flow.md`，再读取已选反思单元。封存 `schemas/reflection-contract.schema.json`，发出 `schemas/reflection-event.schema.json`，以 `schemas/reflection-round-observation.schema.json` 闭合每轮，并使用 `runtime/reflection_runtime.py` 与 `ReflectionSession` 强制准入、绑定指标与证据的基线冻结、提议与授权分离、对象版本跨轮持久、信号与归因证据不可复用、契约批准的归因权威、独立复验、获批验证器基线重建、回归保护、验证器反投机、进展预算、明确停止、已验证回滚事件与独立授权且关联来源的学习决定。契约级观察校验必须传入完整事件流；观察包自哈希不能单独证明历史。只有消费契约回滚绑定、恢复改变前对象且必选验证器绑定恢复证据后，`rolled_back` 才合法。运行时只协调公开证据与状态；不执行修复、不授予行动权限，也不采集私密推理。
   When the selected cell is `reflection-chain`, also read `references/patterns/reflection/reflection-chain.md` and its observability file. Treat the six `schemas/generator-critic-*.schema.json` files as normative and use `runtime/generator_critic.py` with `GeneratorCriticSession` for exact-version artifacts, full criterion coverage, fresh evidence snapshots, supported-finding versus non-gating-opinion buckets, deterministic policy decisions, adopted-issue-only revision, explicit re-review, receipt issuance, and release verification. Build the mandatory guard with `build_shared_reflection_guard`, and map applied artifacts with `reflection_subject_binding_for_artifact`; the adapter validates the complete shared reflection contract, event stream, and round observation, then derives an auditable assurance binding at initial artifact, revision, receipt, and release boundaries. Missing, invalid, exceptional, stale, or artifact-mismatched assurance fails closed. Bind the Generator-Critic contract to the shared reflection contract; keep critic, policy gate, and release gate distinct; cap the chain at one or two review passes; mark every changed artifact unreviewed; and never inherit an old decision or receipt. The in-memory reference is not a production durability claim, and the external publish boundary must be wired to the release guard. / 所选单元为 `reflection-chain` 时，还要读取 `references/patterns/reflection/reflection-chain.md` 及其可观测性文件。以六个 `schemas/generator-critic-*.schema.json` 为规范，使用 `runtime/generator_critic.py` 与 `GeneratorCriticSession` 实现精确版本工件、完整判据覆盖、新鲜证据快照、有据问题与非门控意见分桶、确定性策略裁决、仅消费采纳问题的修订、显式复审、回执签发及发布校验。使用 `build_shared_reflection_guard` 构建必选闸，并以 `reflection_subject_binding_for_artifact` 映射已应用工件；适配器校验完整共享反思契约、事件流与轮次观察，再在初始工件、修订、回执及发布边界派生可审计保证绑定；保证缺失、无效、异常、陈旧或工件不匹配时默认阻断。将生成评审契约绑定到共享反思契约；保持评审器、策略闸与发布闸分离；链限制为一至两次评审；任何内容变化后工件均标记未评审；绝不继承旧裁决或回执。内存参考实现不代表生产持久化，外部发布边界必须接入发布闸。
   When the selected cell is `reflection-routing`, also read `references/patterns/reflection/reflection-routing.md` and its observability file. Treat the eight Skill Package lifecycle Schemas (`skill-package-contract`, `skill-package-candidate`, `skill-package-manifest`, `skill-package-evaluation`, `capability-credential`, `skill-package-alias-receipt`, `skill-package-reuse-receipt`, and `skill-package-event`) as normative; the contract and credential entrypoints are `schemas/skill-package-contract.schema.json` and `schemas/capability-credential.schema.json`. Use `runtime/skill_package.py` with `SkillPackageSession`. Build the mandatory nomination guard with `build_skill_lifecycle_reflection_guard`: shared reflection may emit only an accepted terminal `candidate` decision and cannot grant `VERIFIED`. Keep capability publication separate from runtime invocation; admit every source as `TRIAL`; require distinct verification, credential, publication, and lifecycle authorities; bind exact version, digest, evaluation, environment, tools, and permissions; advance `shadow → limited → production` only with an external CAS receipt; count only real router-selected reuse with external outcome evidence; and suspend the credential plus demote to `TRIAL` before any re-verification. The coordinator is an in-memory semantic reference, not a durable store, evaluator, credential signer, alias mutator, task executor, or proof of operational production qualification. / 所选单元为 `reflection-routing` 时，还要读取 `references/patterns/reflection/reflection-routing.md` 及其可观测性文件。以八个技能包生命周期 Schema（`skill-package-contract`、`skill-package-candidate`、`skill-package-manifest`、`skill-package-evaluation`、`capability-credential`、`skill-package-alias-receipt`、`skill-package-reuse-receipt` 与 `skill-package-event`）为规范；契约与凭证入口分别为 `schemas/skill-package-contract.schema.json` 与 `schemas/capability-credential.schema.json`。使用 `runtime/skill_package.py` 与 `SkillPackageSession`。使用 `build_skill_lifecycle_reflection_guard` 构建必选提名闸：共享反思只能产生已接受且终止的 `candidate` 决定，不能授予 `VERIFIED`。保持能力发布与运行时调用分离；所有来源统一准入 `TRIAL`；要求验证、凭证、发布和生命周期权限主体分离；绑定精确版本、摘要、评估、环境、工具与权限；只在具有外部 CAS 回执时按 `shadow → limited → production` 推进；只统计具备外部结果证据的真实路由选择复用；任何复验前先暂停凭证并降为 `TRIAL`。该协调器是内存语义参考，不是持久存储、评估器、凭证签名器、别名修改器、任务执行器，也不是运行态生产资格证明。
   When the selected cell is `reflection-hierarchy`, read `references/patterns/reflection/reflection-hierarchy.md` and its observability file. Use stable pattern identity `PATTERN_0042` and the shared `experience_replay` reflection route. Keep candidate, recalled, filtered, injected, adopted, beneficial, credited, and lifecycle states separate; current authoritative truth always outranks recalled experience; adoption needs plan or action evidence; benefit and credit need comparable outcome evidence; and every persistent promotion remains separately authorized. The replay receipt and replay-specific stage fields are design-level contracts until a dedicated normative Schema and deployable probe profile exist, so report the cell as `draft` rather than implying validated runtime coverage. / 所选单元为 `reflection-hierarchy` 时，读取 `references/patterns/reflection/reflection-hierarchy.md` 及其可观测性文件。使用稳定模式身份 `PATTERN_0042` 与共享的 `experience_replay` 反思路由。候选、召回、过滤、注入、采用、收益、信用与生命周期状态必须分离；当前权威真值始终优先于召回经验；采用需要计划或行动证据；收益与信用需要可比结果证据；任何持久晋升仍需独立授权。专用规范 Schema 与可部署探针档案尚未建立前，回放凭据及回放专用阶段字段属于设计层契约，因此应将该单元报告为 `draft`，不得暗示已验证的运行时覆盖。
6. For workflow instrumentation, observability gaps, metrics, alerts, or runtime feedback, read `references/workflow-observability-probes.md`; use `runtime/probe_dependency_matrix.json`, `runtime/metric_registry.json`, and `runtime/reasoning_metrics.py`; call `resolve_required_probes` for the selected runtime configuration and `resolve_reflection_required_probes` for an active reflection profile, passing `generator_critic=True` for `reflection-chain` and `skill_package=True` for `reflection-routing`, then preserve field-level provenance plus missing, unknown, not-applicable, observed-zero, computed, and insufficient-sample states. For strict-chain diagnostics, compute registered `candidate_evidence_lineage_integrity_rate` and `readonly_tool_lifecycle_completion_rate` only from complete candidate and dispatch inventories. For Generator-Critic, derive the nine registered `generator_critic_*` metrics only from complete artifact, review, decision, receipt, release, and outcome inventories. For Skill Package, derive the ten registered lifecycle metrics only from complete candidate, package, evaluation, credential, alias, reuse, outcome, and withdrawal inventories; never count shadow, synthetic, pending, unknown, stale-version, or inactive-credential records in a success denominator. Keep aggregate diagnostics non-gating until owned thresholds and promotion evidence are approved; direct exact-version, evidence, authority, receipt, withdrawal-order, or release-integrity violations may still fail closed per instance. / 对工作流埋点、可观测性缺口、指标、告警或运行反馈，读取 `references/workflow-observability-probes.md`；使用 `runtime/probe_dependency_matrix.json`、`runtime/metric_registry.json` 和 `runtime/reasoning_metrics.py`；针对已选运行配置调用 `resolve_required_probes`，针对活动反思档案调用 `resolve_reflection_required_probes`，`reflection-chain` 传入 `generator_critic=True`，`reflection-routing` 传入 `skill_package=True`，并保留字段级来源以及缺失、未知、不适用、观测零值、已计算和样本不足状态。严格链诊断仅在候选与分派清单完整时计算已注册的 `candidate_evidence_lineage_integrity_rate` 与 `readonly_tool_lifecycle_completion_rate`。生成评审的九项已注册 `generator_critic_*` 指标只能从完整工件、评审、裁决、回执、发布及后验清单派生。技能包的十项已注册生命周期指标只能从完整候选、技能包、评估、凭证、别名、复用、结果与撤权清单派生；影子、合成、待定、未知、陈旧版本或非活跃凭证记录绝不计入成功分母。在负责人阈值和晋升证据获批前，聚合诊断保持非门控；精确版本、证据、权限、回执、撤权顺序或发布完整性的直接违规仍可按实例默认阻断。
   For parallel diagnostics, call `project_parallel_run(plan, events)` and compute registered `candidate_completion_rate`, `branch_diversity`, and `branch_record_completeness` only from its immutable planned-path, candidate-binding, and terminal-record inventories. Preserve projector anomalies and projection hash with the metric window. Keep these metrics and `path_convergence_rate` non-gating until accountable owners approve thresholds, minimum samples, drift controls, and promotion evidence; planned synthesis-fidelity or silent-drop metrics must remain unreported rather than fabricated. / 并行诊断调用 `project_parallel_run(plan, events)`，并且只基于其不可变计划路径、候选绑定和终态记录清单计算已注册的 `candidate_completion_rate`、`branch_diversity` 与 `branch_record_completeness`。投影异常与投影哈希必须随指标窗口保留。这些指标及 `path_convergence_rate` 在责任人批准阈值、最小样本、漂移控制与晋升证据前保持非门控；规划中的综合保真或静默丢弃指标不得伪造成已观测结果。
   Compute `outcome_linkage_coverage`, `underroute_rate`, `overroute_rate`, `route_abstention_rate`, `route_oscillation_rate`, and `forced_route_with_missing_signal_rate` only from complete registered denominators and explicit exclusions. These route metrics, including `route_stability_rate`, remain diagnostic until an accountable owner approves thresholds, minimum samples, drift controls, and promotion evidence; missing outcome or switch-chain data remains missing rather than zero. / 仅基于完整注册分母与显式排除项计算后验关联覆盖、欠路由、过路由、弃权、振荡及缺失信号强制路由指标。这些路由指标（包括路由稳定率）在责任人批准阈值、最小样本、漂移控制与晋升证据前均保持诊断；后验或换路链数据缺失时保留为缺失，不能记为零。
7. For Harness source, read `references/harness-source-analysis.md` and run Detect / Classify / Filter / Map / Verify. / 对 Harness 源码，读取 `references/harness-source-analysis.md`，执行找主循环、归类、过滤、映射和验证。
8. For pattern or Skill packaging, read `references/pattern-skill-packaging.md`. / 对模式或 Skill 封装，读取 `references/pattern-skill-packaging.md`。
9. For quality, evidence, risk, and governance, read `references/evaluation-governance.md` and `references/failure-modes.md`. / 对质量、证据、风险和治理，读取 `references/evaluation-governance.md` 与 `references/failure-modes.md`。
9. For controlled extension, read `references/extension-rules.md`; for an end-to-end example, read `references/example-compilation.md`. / 对受控扩展读取 `references/extension-rules.md`；需要端到端示例时读取 `references/example-compilation.md`。

## Automatic Engineering Node Analysis / 工程节点自动分析

When the request is to analyze an engineering node, automatically run Trace Insert / 自动运行 Trace 插入 before any matrix decision. / 当请求是分析工程节点时，在任何矩阵决策前自动运行 Trace 插入。

Collect the node responsibility, owner, boundary, trigger, inputs, outputs, current behavior, failure signals, risk, and available evidence. Then run ASSESS / 评估, ROUTE / 判拓扑, and SELECT / 查矩阵 using `references/pattern-selection-card.md`. / 采集节点职责、负责人、边界、触发、输入、输出、当前行为、失败信号、风险和可用证据；然后使用 `references/pattern-selection-card.md` 执行评估、判拓扑和查矩阵。

If evidence is incomplete, return a preliminary / 初步 result with gaps and verification tasks. Block only final pattern promotion or high-confidence selection; do not suppress useful preliminary analysis. / 如果证据不完整，输出带缺口和验证任务的初步结果；只阻断最终模式晋升或高置信选型，不压制有用的初步分析。

## Workflow And Node Diagnosis / 工作流与节点诊断

1. Read `references/workflow-nodes.md` and split by business responsibility, input, output, owner, decision, and risk. / 读取 `references/workflow-nodes.md`，按业务职责、输入、输出、负责人、决策和风险拆分节点。
2. Read `references/axes.md`, then select the smallest capability and topology that preserve required dependencies. / 读取 `references/axes.md`，选择能保留必要依赖的最小能力与拓扑。
3. Read `references/matrix-index.md`, the relevant `references/patterns/<capability-key>/cell.md`, the selected design file, and its observability file. / 读取 `references/matrix-index.md`、相关纵轴导论、已选设计文件及其可观测性文件。
4. Read `references/pattern-catalog.md` for named candidates and `references/diagnosis-method.md` for concrete workflow modification. / 使用 `references/pattern-catalog.md` 查命名候选，使用 `references/diagnosis-method.md` 形成具体工作流修改。
5. Cite evidence for every architecture judgment and name at least one failure mode, mitigation, verification method, and observation point. / 每个架构判断都引用证据，并至少命名一个失败模式、缓解方式、验证方法和观察点。

## Harness Source Compilation / Harness 源码编译

1. Detect the main loop and control plane before peripheral implementation details. / 先定位主循环和控制面，再读边缘实现细节。
2. Classify retained components into perception, memory, reasoning, action, reflection, collaboration, and governance. / 将保留组件归类到感知、记忆、推理、行动、反思、协作和治理。
3. Keep files that change context, state, tools, permissions, execution, retry, audit, or handoff; defer boilerplate until needed. / 保留会改变上下文、状态、工具、权限、执行、重试、审计或交接的文件；样板内容按需延后。
4. Map each component to one primary coordinate, add secondary coordinates only when they change the diagnosis, and attach evidence before extracting patterns. / 将每个组件映射到一个主坐标；只有副坐标会改变诊断时才补充，并在抽取模式前挂载证据。

## Output Profiles / 输出档位

Choose the smallest profile that satisfies the request. / 选择能满足请求的最小档位。

### quick / 快速

Return coordinate, evidence, diagnosed problem, practical adjustment, risk, and verification. / 输出坐标、证据、诊断问题、可执行调整、风险和验证方式。

### standard / 标准

Return an EIR slice, node or component map, ASSESS / ROUTE / SELECT result, selected patterns, observability metrics, risks, governance, verification, and a project-local Trace proposal. / 输出 EIR 切片、节点或组件图、评估/判拓扑/查矩阵结果、已选模式、可观测性指标、风险、治理、验证和项目本地 Trace 建议。

### full / 完整

Return the complete compiler output: scope, main flow, EIR, noise filter, matrix mapping, pattern and Skillization candidates, evidence verification, evaluation scores, governance gaps, failure modes, extension analysis, and implementation plan. / 输出完整编译结果：范围、主流程、EIR、噪声过滤、矩阵映射、模式与 Skill 化候选、证据验证、评价分数、治理缺口、失败模式、扩展分析和实施规划。

## Runtime Trace / 运行 Trace

Read `references/trace-schema.md`. Normal use produces a Trace proposal at `.harness-analysis/<analysis_id>/trace.yaml`. / 读取 `references/trace-schema.md`。普通使用在 `.harness-analysis/<analysis_id>/trace.yaml` 生成 Trace 建议。

Bundled `references/patterns/*/trace.md` files are curated historical snapshots, not runtime state. Modify curated history only when the user explicitly requests a Skill evidence update and the evidence has been reviewed. / Skill 内置 `references/patterns/*/trace.md` 是精选历史快照，不是运行状态。只有用户明确要求更新 Skill 证据且证据经过复核时，才能修改精选历史。

If the project-local destination is unavailable, return the Trace payload in the response with the intended path; never redirect runtime data into bundled history. / 如果项目本地目标不可用，在响应中返回 Trace 载荷和预期路径；绝不把运行数据重定向到内置历史。

## Constraints / 约束

- Keep all Skill metadata and core instructions bilingual in Chinese and English. / 所有 Skill 元数据和核心说明保持中英双语。
- Keep newly created Skill resources under `.\skills`. / 新建 Skill 资源统一放在 `.\skills` 下。
- Keep stable IDs and registry records after introduction; deprecate instead of reusing. / 稳定 ID 与注册表记录引入后保持不变；使用废弃标记，不复用。
- Treat `registry.json` as source data and Markdown matrices as maintained views. / 将 `registry.json` 视为源数据，将 Markdown 矩阵视为维护视图。
- Cite source, test, official documentation, config, log, Trace, runtime record, or protocol evidence for every architecture judgment. / 每个架构判断都引用源码、测试、官方文档、配置、日志、Trace、运行记录或协议证据。
- Treat README evidence as supporting evidence, not the strongest proof. / README 只作为辅助证据，不作为最强证明。
- Prefer controlled extension over forcing a workflow into an inaccurate cell. / 优先受控扩展，不强行把工作流塞入不准确的单元。
- Do not invent patterns to fill empty cells. Promote a candidate only with repeated evidence, failure-path checks, and explicit provenance. / 不为填满空白单元而虚构模式；只有存在反复证据、失败路径检查和明确来源时才晋升候选。
- After changing reasoning contracts, runtime behavior, probes, or metrics, run the schema, runtime, metric, registry, and full test suites; a structural marker pass alone is not runtime verification. / 修改推理契约、运行时行为、探针或指标后，必须运行 Schema、运行时、指标、注册表及全量测试；仅结构标记通过不代表运行时已验证。
